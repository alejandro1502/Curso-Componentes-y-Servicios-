import { ReplaceLetterPipe } from './replace-letter.pipe';

describe('ReplaceLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
